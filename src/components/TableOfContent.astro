---
import {MarkdownHeading} from "astro";

type Props={
    headings: MarkdownHeading[];
}

const {headings} = Astro.props;
---

<nav>
    <ul>
        {headings.map(heading => <li class="text-zinc-500 text-xl mb-4"><a href={`#${heading.slug}`}>#{heading.text}</a></li>)}
    </ul>
</nav>
